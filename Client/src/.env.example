# REQUIRED — must point at the Flask /api prefix
# Your logs showed GET /places 404 — that means /api was missing.
# Correct:
VITE_API_BASE_URL=http://localhost:5000/api

# Optional
VITE_MAPBOX_ACCESS_TOKEN=
