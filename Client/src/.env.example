# REQUIRED — must include the Flask /api prefix
# Local:
VITE_API_BASE_URL=http://localhost:5000/api

# Production example (Render):
# VITE_API_BASE_URL=https://emspot-api.onrender.com/api

# Optional
VITE_MAPBOX_ACCESS_TOKEN=
